<%= form_for(@general_educator) do |f| %>
  <% if @general_educator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@general_educator.errors.count, "error") %> prohibited this general_educator from being saved:</h2>

      <ul>
      <% @general_educator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
